# 2026-09-06 播放链排查与顺播

用户确认：只有 KING 播放音乐时出现电流/机械失真，其他播放器没有。暂停后恢复会暂时正常，不能据此认定修复。保持软件标准总音量 66，不修改 Qu-16/DP448 参数，AI 制作与补音关闭。

## 当前证据

- 旧安装程序为 2026-09-04 构建，当前运行进程始于 09-05；两个 mpv 独立 WASAPI shared 会话使用 Qu-16 ST3，同为强制 48 kHz、stereo、0.5 秒缓冲。
- 检查时 Deck 1 文件 48 kHz，Deck 2 文件 96 kHz；输出强制 48 kHz。并非发现第二条灯光音频输出：节拍读取波形数据，视频色彩读取静音画面。
- Qu 驱动界面为 48 kHz、ASIO 512 samples、Safe Mode，ASIO 当时未激活。此次未修改驱动或系统电源设置。
- 观察时无持续 CPU/GPU/内存压力。该结果不能排除瞬时驱动调度问题，也不能证明音响输出干净。
- 临时只读事件记录保存于本机 Temp/king-audio-diagnostics-20260906，记录控制与播放器状态，不含音频录音。

## 本次实现

1. 阻塞式 mpv 控制从 Tauri UI 线程迁至 spawn_blocking。
2. 淡化 set_volume 移除每次额外五个状态读请求，只返回命令确认。
3. 原唱/伴奏换源补齐先静音暂停、真实新路径稳定、精确定位稳定、再渐入恢复的流程。
4. 正常状态查询将 IPC 错误向上传递，不制造假的暂停/零进度/EOF。
5. 每个 mpv 增加一个只读事件订阅；日志目录 `%APPDATA%/club.king.broadcast-control/runtime/audio-diagnostics`，每 Deck 当次上限 8 MiB，保留前次日志。日志可发现播放器报告的错误和格式变化，不能等同声学电流声检测。
6. 视频默认顺序循环，Take 时绑定分类队列，末尾接首项。PVW 与浏览分类不改变 PGM；输出窗口单一结束事件，token 去重。视频音轨默认静音。
7. Titan 连接失败保留具体错误文本，不再仅显示泛化离线提示；未凭 HTTP 可达便认定现场灯光已恢复。

## 验证

- 51 项 JavaScript 回归通过，覆盖视频顺序/回绕/删除跳过/重复结束/分类隔离及音频控制安全约束。
- Rust mpv 相关 11 项通过，包含真实文件装载、播放、定位、原唱换源；测试编译强制 `--ao=null`，没有输出现场音响。
- 前端生产构建及 Tauri release 构建通过；安装目录程序已替换，保留此前 exe 到 `artifacts/desktop-checkpoints/20260906-audio-video/king-broadcast-control.previous.exe`。最终安装 SHA256：`B9318E89C08AE5A8E3BD0F24B0F10EE02B809046E03FD946A46FF2001F50ABFA`。
- 真实原生输出窗口验证了当前 216 段视频队列的下一段和末段回首段，使用视频真实 EOF，不是伪造 ended 事件；预选/浏览另一分类没有改写队列，重复旧 token 被拒绝。整个测试视频静音，两 Deck 保持原状态；结束恢复黑屏及首页单监看。
- 重启核对软件总量 66、两个 Deck `sequence`、视频 `sequence`、视频音轨静音、AI 制作关闭。两套曲库的周日列表均为空，启动未越界选取全曲库或其它分类。
- Titan TT-00608 自动连接，Show 2024.12.28，122 个 Playback；保持自动联动开启、光束未布防。现场实际节拍/色彩观感仍需在播放时确认。
- 修复并实测日志首次连接竞争：日志中已收到两个 Deck 的真实播放器 property-change/命令确认事件。仅有 UI 电平或播放器无报错，不能证明没有可听失真；尚未完成更新后现场长时间听感验收。

实现依据：[Tauri 官方命令线程说明](https://v2.tauri.app/develop/calling-rust/)、[mpv 官方 IPC 与日志协议](https://mpv.io/manual/master/)。

上述代码缺陷已修正，不代表已查明电流声唯一根因；必须进行实际输出连续播放验收，发生异常时与播放器日志和输出链采样对时。

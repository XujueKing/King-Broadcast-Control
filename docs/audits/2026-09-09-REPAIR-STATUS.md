# 审计修复与验证 · 2026-09-09

对应原报告：[2026-09-09-GPT6-SOFTWARE-AUDIT.md](./2026-09-09-GPT6-SOFTWARE-AUDIT.md)。

本轮在 `main` 上修复已确认的问题，未创建分支。下表区分源码/本机验证和现场验收；本轮没有开启主扩、操作 Qu-16 参数或向物理灯具发送测试输出。

## 问题处理

| 审计项 | 本轮处理 | 验证和边界 |
| --- | --- | --- |
| F01 · 播放器控制阻塞 | mpv 命名管道使用 Windows overlapped I/O，连接/写入/读取共用 800ms 请求截止时间；Deck 1/2 和参考补音分别持锁。人声控制通道使用独立读写线程，2 秒未响应则终止失联子进程。退出直接停止所属播放器进程。 | 静默命名管道、失联子进程、跨 Deck 锁隔离测试通过；真实 mpv 装载/播放/跳转/音源切换通过，测试使用 null 音频输出。 |
| F02 · 暂停后继续发送旧灯光指令 | 引入单调 generation；播放状态、歌曲、模式、设备或联动开关改变后作废旧任务。后端在网络写入前、等待期间和读取期间检查取消。收光绕过前端普通队列，失败不再显示为成功。已发送但未确认的 Playback 在错误/取消后尝试释放。 | 6 秒等待被取消的回归测试要求 300ms 内返回；旧任务不能再进入执行。已进入操作系统/设备的指令无法撤回，网络故障时只能报告“收光/释放未确认”。 |
| F03 · 启动改变显示拓扑 | 删除启动时的 `DisplaySwitch.exe /extend` 和固定等待，继续读取当前可用显示器并保持主屏排除规则。 | 新版桌面启动正常；单屏下独立输出窗口隐藏、节目恢复黑屏。 |
| F04 · 跟拍速度参数丢失 | `speedValue` 贯穿节拍计算、Tauri 参数和 Fixture 42 速度写入。先做亮度脉冲和复位，再处理速度；速度写入合并到最多约一次/秒，降到慢歌速度区间允许立即更新。 | 80 BPM 参数传递及速度合并测试通过。不是逐拍追加完整发现/选灯队列；物理延迟待现场测量。 |
| F05 · 取色失败不再重试 | 色彩状态分成观察值、待确认请求和已成功应用值；失败可重试，过期响应不能覆盖当前状态。基线/布防标记只在成功后写入。 | 失败重试、颜色更替及旧响应回归通过。 |
| F06 · 保存失败显示成功 | 捕获存储异常并保留未保存状态；保存成功才更新保存时间。当前运行期间跨页面/歌曲保留草稿，编辑后关闭时提示未保存。 | 在真实桌面 WebView 注入存储配额异常：显示“保存失败”，随后正常保存和跨页面未保存裁切恢复均通过；测试存储已恢复原值。 |
| F07 · 示例时钟和节目画面 | 本地预览时钟推进、暂停、跳转和结束；V1/V2/图片/文字按实际片段渲染，视频始终静音。PGM 使用真实节目内容及独立输出窗口的实际播放时钟；时间尺与片段宽度统一按歌曲时长。删除假准备、帧率等状态。未接入的效果、灯光执行、撤销/吸附明确禁用。 | 本地真实视频播放到 1.3 秒以上且静音；节目输出 31.38 秒时编排监看 31.20 秒，返回首页 32.17 秒，未从头播放。全程未启动 Deck 或调用物理输出控制。完整现场编排执行引擎仍未实现。 |
| F08 · 无效源裁切 | 源时长与歌曲时长分离；入点最大为源时长减 0.1 秒，出点必须大于入点；视频元数据加载后校正实际源时长。音轨锁定更新被拒绝。 | 12 秒素材及 3 秒时长校正测试通过；桌面输入超界值后限制为 29.9–30 秒。 |
| F09 · 每次启动重复解包 | 导入放到后台。成功导入保存包及已发布文件的大小/修改时间和数据库就绪记录，未变化时跳过解包。缓存失效后重新校验原唱及已发布制作文件，冲突报错，不覆盖用户内容。 | 重复导入命中、缺失记录重新验证、制作文件改变后拒绝误报成功均通过。未做酒吧真实大曲库冷启动压力测试。 |
| F10 · 交付环境不完整 | 打包前编译并纳入 `king-vocal-engine.exe`；AI 环境支持显式环境变量、程序旁和 AppData 的目录。区分 GPU、环境文件和人声程序可用性；没有环境时不能按硬件标识启动 AI。 | MSI 文件表确认包含人声程序；运行时 `vocalEngineAvailable=true`。当前机 CUDA/BF16/分离模型存在，MOSS 服务未运行，整体 AI 自检 `ok=false`。Python/模型不内嵌，见部署说明。 |
| F11 · 运行旧包 | 重新构建前端、Rust 主程序及 MSI/NSIS，生成提交、源码树和人声程序指纹，读取运行中的 `build-info.json` 核对。 | 最终源码树指纹与桌面运行资源一致；打包期间曾遇文件占用，关闭测试实例后重新生成最终 NSIS。 |
| F12 · 依赖漏洞/网络暴露 | Vite 固定到 6.4.3，更新受影响的锁定依赖，开发服务器默认绑定 127.0.0.1。 | 完整 npm audit 为 0 个已知漏洞；前端构建和测试通过。此结果不是对未知漏洞的保证。 |

## 本轮测试

| 检查 | 结果 |
| --- | --- |
| 前端离线回归 | **171/171 通过**；排除依赖桌面会话的 `tauri-*` 和媒体浏览器集成测试。 |
| 桌面 Rust 单元/协议/故障测试 | **122/122 通过**。 |
| 真实 mpv fixture 集成测试 | **1/1 通过**，明确提供两个本地音频 fixture，未走无 fixture 的跳过路径；使用 `--ao=null`。 |
| 桌面编排集成 | 保存失败/重试、实际源裁切、真实静音视频预览、草稿跨页面恢复、PGM 保持黑屏均通过；未发出 Deck 或 Titan 控制调用。 |
| 节目监看集成 | 真实独立输出视频时钟、切到编排页和返回首页同步通过；所有视频静音、两个 Deck 未装载，结束恢复黑屏。 |
| npm audit | **0 vulnerabilities**。 |
| AI 环境自检 | Python 3.12.10、PyTorch 2.11.0+cu128、RTX 5090 Laptop、CUDA/BF16、分离模型检查通过；**MOSS 未运行，整体未通过**。 |
| 安装包资源 | MSI 文件表中包括主程序、mpv、libmpv、KGMA 解码器、人声程序和许可证。 |

原审计的人声 Rust 126 项及 Python 24+3 项结果保留在原报告中。本轮没有改变其算法代码，也没有将旧结果冒充本轮重新执行。

详细本机证据位于忽略目录 `tmp/fix-audit-20260909/`：`rust-tests.log`、`js-tests.log`、`real-mpv.log`、`desktop-qa.json`、`program-clock-qa.json`、`ai-preflight.json`、`npm-audit.json`、`installer-files.json` 和打包日志。

## 交付与后续

[部署说明](./2026-09-09-DEPLOYMENT-NOTES.md) 列出播放电脑部署、AI 环境定位和自检步骤。

尚需现场完成：i5-10400F / 8GB / RX550 机器稳定性与资源实测、已核验 Fixture 42 的真实音乐视觉响应、实际音频输出听感/回录，以及需要使用制作功能时的 MOSS 和真实制作任务验收。软件测试不能替代这些结果。

本轮修复通过 `main` 交付；最终运行实例和安装包指纹见下方交付记录。构建时提交记录为修复前的基线，实际修复源码以源码树指纹核对。

## 最终交付记录

- 分支：`main`，源码基线提交：`9ec9ba47283cd727dea4c24972f86ec10cf3705e`。
- 源码树 SHA-256：`ab2ea05f82b601090b81e2d7138780d8cc956a432d32651ff229dffe1321a75b`。
- 嵌入的前端资源：`styles-Ct1GHFum.js`，与当前构建目录一致。
- 最终运行进程：PID **15520**，正常启动参数；调试实例已关闭。

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| [king-broadcast-control.exe](D:/WEB3_AI/KINGCLUB-Broadcast-Control/ui-prototype/src-tauri/target/release/king-broadcast-control.exe) | 24108032 | `2c0d0f39154f40628a0ecf3050fa8b951393bbefa4302224eecb38320174313f` |
| [KING CLUB Broadcast Control_0.1.0_x64-setup.exe](<D:/WEB3_AI/KINGCLUB-Broadcast-Control/ui-prototype/src-tauri/target/release/bundle/nsis/KING CLUB Broadcast Control_0.1.0_x64-setup.exe>) | 81628127 | `c831b41f8681afe3798f05f32d41bc11e47f9b99e02ea4e23f40c841396a1139` |
| [KING CLUB Broadcast Control_0.1.0_x64_en-US.msi](<D:/WEB3_AI/KINGCLUB-Broadcast-Control/ui-prototype/src-tauri/target/release/bundle/msi/KING CLUB Broadcast Control_0.1.0_x64_en-US.msi>) | 110563328 | `717660ab2982aec1416988d0ade3c3b9ec64d2ff23c50b1ea0021c2c6803943c` |
| [king-vocal-engine.exe](D:/WEB3_AI/KINGCLUB-Broadcast-Control/ui-prototype/src-tauri/target/release/king-vocal-engine.exe) | 1046528 | `d31727d1f783f20d92cd0a2f3ad6baedc5b209acac143a7451c7f027e72757ee` |

NSIS 最终打包退出码为 0。重打包曾提示未找到未写入的 Bundle Type 标记；核对本地 tauri-utils 2.9.3 源码及主程序字节，确认二进制已含唯一的 `__TAURI_BUNDLE_TYPE_VAR_NSS`，而非未知或 MSI 标记。这是对已标记产物再次打包的提示；本项目未启用 updater 插件。

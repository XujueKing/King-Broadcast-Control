# 产品需求

状态：首版范围讨论中

版本：0.2

## 1. 产品目标

为 KING CLUB 提供一套运行在 Windows 主控电脑上的本地播控软件，统一完成视频、音频、LED 大屏、调音台和灯光场景的日常控制。系统必须适合营业现场快速操作，并为后续手机点歌、弹幕、祝福文字和多屏控制预留能力。

## 2. 已确认现场条件

### 操作端

- Windows 主控电脑
- LED 主屏通过现有视频处理系统即插即用，作为普通 Windows 第二显示器连接
- 操作员在主控界面查看实时预览并执行播控

### 显示设备

- 舞台异形 LED 主屏一块
- 已确认总外框：宽 5120 mm × 高 5760 mm
- 下部 B 区：宽 5120 mm × 高 2880 mm
- 上部 A 区：宽 2560 mm × 高 2880 mm，位于 B 区上方中央
- P2.5 LED 模组：320 × 160 mm，单模组 128 × 64 px，1/32 扫描
- B 区为 16 × 18 个模组，对应 2048 × 1152 px
- A 区为 8 × 18 个模组，对应 1024 × 1152 px
- 外框逻辑画布推算为 2048 × 2304 px，上半区左右各有 512 × 1152 px 的非显示区域
- 实际发光像素约 354 万
- 现场已经能够直接播放普通 Windows 视频，画面可覆盖整个异形发光区域
- DJ 台另有一块长条 LED 小屏，首版是否接入待确认
- 架构需预留多显示输出，但首版以主屏稳定输出为优先

注意：以上像素布局由模组参数推算。软件不直接配置 LED 模组或发送卡，而是把现有 LED 视频处理系统当作普通 Windows 显示器使用。首次启动时自动读取 Windows 报告的实际分辨率、刷新率、显示器标识和位置并保存；操作员可在设置中重新选择输出屏。

### 音频设备

- Allen & Heath Qu-16 数字调音台
- 软件需要播放歌曲音频
- 需要像 KTV 一样切换原声/伴唱
- 需要控制与歌曲播放相关的调音台功能，具体通道和动作待确认

### 灯光设备

- Avolites Tiger Touch Pro 灯光控制台
- 灯光节目继续由灯光台保存和执行
- 软件需要为歌曲绑定预设灯光，并在歌曲开始或人工点击时触发

## 3. 首版用户

主要用户暂定为主控电脑前的现场操作员。账号、多人操作和权限暂不作为首版核心，但危险操作仍需要防误触。

后续用户包括顾客或工作人员使用手机完成点歌、发送弹幕及生日/欢迎祝福。

## 4. 首版核心流程

### 4.1 营业日节目

1. 操作员选择星期一至星期日对应的固定节目模板。
2. 系统加载当天歌单。
3. 每首歌曲可关联歌曲音频、视频背景和灯光预设。
4. 操作员可临时调整顺序或切换播放模式。
5. 播放时同步执行已绑定的视频、音频与灯光触发。

需要进一步确认：节目是按星期保存一份固定歌单，还是每个星期模板下还可保存多个版本。

### 4.2 歌曲播放

- 查看当前歌曲和后续歌单
- 播放、暂停、继续、上一首、下一首
- 单曲循环、顺序播放、随机播放
- 原声/伴唱切换
- 查看歌曲关联的视频和灯光预设
- 临时覆盖某首歌曲的关联项

### 4.3 视频播放

- 浏览本地视频库
- 缩略图/名称搜索与分类
- 点击视频直接在主屏播放
- 主控界面显示低延迟实时预览
- 支持暂停、继续、停止、循环
- 支持异形屏安全区/遮罩，避免内容落入无 LED 区域
- 支持纯色视频、待机画面和紧急黑场

### 4.4 灯光控制

- 展示已配置的灯光预设组合
- 点击预设触发 Tiger Touch Pro 内已编程的 Cue/Playback
- 歌曲可绑定一个默认灯光预设
- 自动触发失败时应提示，并允许人工重试或改选预设
- 首版不直接接管灯具 DMX/Art-Net 输出，避免与灯光台争用控制权

### 4.5 大屏文字

- 输入文字并显示到主屏
- 支持欢迎语、生日祝福和临时通知
- 至少支持字体大小、颜色、位置、持续时间和进入/退出方式
- 操作端发送前可预览
- 手机弹幕属于后续阶段，需要内容审核、频率限制和一键关闭

### 4.6 音频与调音台

- 播放歌曲音频并跟随歌单控制
- 切换原声与伴唱音轨
- 向 Qu-16 发送已批准的控制动作
- 首版建议仅开放必要动作，例如指定通道静音、音量或场景调用
- 软件显示的调音台状态应尽量与设备实际状态双向校准

## 5. 功能优先级

### P0：可完成首版验收

- Windows 单机运行，断开公网仍可播控
- 主屏全屏输出与主控实时预览
- 本地歌曲、视频素材库
- 星期节目模板与歌单
- 播放控制及循环/顺序/随机模式
- 原声/伴唱切换
- 歌曲绑定视频与灯光预设
- 手动视频、灯光预设和文字投放
- 设备连接状态、失败提示、日志与紧急黑场

### P1：首版稳定后

- DJ 台长条小屏独立输出
- 更丰富的转场、图层和文字模板
- 节目导入导出、备份与恢复
- 调音台更多参数和场景控制

### P2：移动互动

- 手机扫码点歌
- 手机发送弹幕、欢迎语和生日祝福
- 内容审核队列、敏感词、频控和黑名单
- 多屏独立编排

## 6. 非功能要求

- 核心播放和控制不依赖公网
- 软件异常不能把 Windows 桌面或报错窗口暴露到 LED 主屏
- 主屏播放进程异常时应进入安全画面或黑场
- 危险操作需要明确反馈和适当确认
- 每条外设指令记录发送时间、目标、结果和错误原因
- 设备断线后能自动检测，恢复连接后校准状态
- 支持营业时长的连续稳定运行；具体压力测试时长待确认

## 7. 下一轮必须确认

1. 歌曲文件如何实现原声/伴唱：同一文件双声道、两个独立文件，还是视频内多音轨？
2. Qu-16 当前固件版本、网络连接方式、IP 地址，以及要控制的通道/场景分别是什么？
3. Tiger Touch Pro 的序列号、Titan 软件版本、网络连接方式，以及现有灯光预设保存在哪些 Playback/Cue？
4. DJ 台长条屏的像素分辨率、连接方式，以及首版是否必须显示独立内容？
5. 常用媒体格式、最高分辨率/帧率、单文件大小和素材总量是多少？
6. 星期节目切换是人工选择，还是到时间自动切换？节目是否包含具体播放时刻？
7. “下一首”时视频、音频、灯光应同时硬切，还是分别使用淡入淡出时间？
